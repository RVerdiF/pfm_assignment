Evidence — Card 3 (t_485f334d) — Narrative, architecture and walkthrough completion
Branch: wt/t_485f334d  (pushed to RVerdiF/pfm_assignment)
Base:   origin/main 3a83509 (Card 2 merged via PR #18)
Commits:
  5d0e617 feat(streamlit): complete walkthrough narrative, architecture
          and methodology (Card 3)
  f3b97c8 docs(streamlit): pin Card 3 commit SHA in evidence file
  296035e fix(streamlit): derive methodology narrative numbers live (rework)

Rework round (review gate — data/reporting correctness)
-------------------------------------------------------
Reviewer finding: streamlit/sections/methodology.py hard-coded every reported
result (100/0/100, 92, £1,001.70, the 13/40/47 loss breakdown) in
RESULT_INTERPRETATIONS, contradicting the module contract and the documented
PFM_DUCKDB_PATH existing-warehouse override, for which a warehouse with
different totals would make the live caption and the narrative disagree.

Fix applied:
* streamlit/sections/methodology.py
    - Removed the hard-coded RESULT_INTERPRETATIONS and RECOMMENDATIONS
      tuples.
    - Added _read_narrative_facts(connection): one read of the same three
      dbt relations the analysis page charts — marts.mart_attribution_health
      (decided/match-status sums), marts.fct_revenue_attribution (valid
      conversions and commission), and intermediate.int_unmatched_conversions
      (loss-cause reason counts, the ADR-8 diagnostic view).
    - _build_result_interpretations(facts) renders each interpretation only
      when the live numbers make it true (an all-unmatched warehouse gets the
      full reading; a warehouse with matches does not claim the opposite) and
      quotes the live decided/valid/commission/reason counts.
    - Recommendations now append their loss-cause counts as live tails ("In
      this sample, N conversions ...") only when that cause is present.
    - render() reads the facts once and passes them to the results and
      recommendations sections, so every number cited on the page is
      internally consistent within a render and can never drift from the
      warehouse it is served against.
* README.md + docs/decisions.md (ADR 9 consequences)
    - Document that every narrative quantity (health-mart sums, revenue-valid
      conversions/commission, loss-cause reason counts) is read live from the
      same dbt relations as the charts, so the PFM_DUCKDB_PATH override can
      never be contradicted by the walkthrough.
* tests/test_methodology_page.py
    - Added reconciliation tests whose expected values are read from the same
      relations (never hard-coded):
        * test_methodology_prose_reconciles_with_live_marts — the default
          real warehouse: caption + prose must equal the health-mart sums,
          revenue-valid totals/commission, and the reason taxonomy.
        * test_methodology_narrative_renders_against_non_default_warehouse —
          regression with a *different valid warehouse* (12 decided, 3
          matched, 4/3/2 loss reasons, £270.75 commission) via
          PFM_DUCKDB_PATH; proves the narrative switches to those live totals
          and never leaks the delivered-sample claims (no "£1,001.70", no
          "100 conversions"). Requires clearing st.cache_resource between
          AppTest runs so the override path is actually re-resolved.
    - The method/limitations structural checks are unchanged.

Scope delivered (all criteria map to code under streamlit/ + tests + docs)
--------------------------------------------------------------------------
* streamlit/sections/overview.py  — first screen now explains the purpose:
    - "The problem" prose kept and extended with a "Purpose of this
      walkthrough" block (what / how / how much / what it means).
    - Architecture: a real client-side graphviz diagram
      (ARCHITECTURE_DOT, st.graphviz_chart — no server-side graphviz binary,
      rendered by the browser) showing the exact pipeline
      Excel sample -> Polars -> DuckDB raw -> dbt staging -> dbt intermediate
      -> dbt marts -> Streamlit, plus ARCHITECTURE_LAYERS with one-line
      responsibility per layer. Node labels match the real repo layout.
* streamlit/sections/methodology.py  — final page replaces the Card-1
  placeholder with four sections, all prose-first:
    - Attribution method: the four deterministic rules in plain language
      (1. exact match only, 2. temporal window, 3. identifier priority,
      4. recency tie-break) + the matched/ambiguous/unmatched outcomes.
      Vocabulary matches int_conversion_attribution.sql and winner_rules.yml.
    - Interpreting the observed results: prose reading of the real sample
      (0/100 matched, where loss occurs: 47 outside window / 40 not found /
      13 missing click id, £1,001.70 fully unattributed), every number read
      live from marts.mart_attribution_health, marts.fct_revenue_attribution
      and intermediate.int_unmatched_conversions so text cannot drift.
    - Limitations: small sample, limited PostHog window, conversion date
      without time, no authoritative daily commission, no fuzzy/undocumented
      bridges.
    - Recommendations: raise click-id coverage, propagate identifiers,
      persist between session and conversion, widen + monitor the window;
      each cites the live loss cause it addresses.
* streamlit/sections/analysis.py  — each of the four Card-2 sections now
  opens with a textual interpretation (Reading the cards / Where the loss
  happens / Reading the charts / Reading the chart) driven by the same
  dbt relations the visuals use; the numbers are never hard-coded.
* streamlit/sections/_components.py  — removed the now-dead
  placeholder_section() helper (no caller remains).
* docs/decisions.md — ADR #9: narrative self-contained in Streamlit pages;
  layer responsibilities/rule vocabulary kept in sync with dbt headers.
* README.md — walkthrough page narrative updated (architecture diagram +
  prose readings + methodology page responsibilities).

Tests (all hermetic or real-warehouse AppTest, no mock of the pipeline)
------------------------------------------------------------------------
* tests/_overview_driver.py + tests/_overview_page.py   — 3 AppTest render
  checks against the REAL built worktree warehouse (PFM_DUCKDB_PATH): no
  exception; title + three section headers; problem stated in prose; the
  architecture diagram is rendered and its DOT payload carries every real
  stage label (Ingestion (Polars), DuckDB raw, dbt staging, dbt
  intermediate, dbt marts, Streamlit) so a stale diagram cannot pass.
* tests/_methodology_driver.py + tests/_methodology_page.py — AppTest render
  checks: no exception; four section headers; method vocabulary (Exact match
  only / Temporal window / Identifier priority / Recency tie-break /
  matched / ambiguous / unmatched); every limitation and recommendation
  listed; narrative reconciliation against the real marts; and the
  non-default PFM_DUCKDB_PATH regression proving the narrative switches to
  that warehouse's totals.
* Full suite: 45 passed (37 pre-existing + 3 overview + 5 methodology).

Acceptance verification (real executions)
-----------------------------------------
1. Warehouse built in the isolated worktree: ingestion + `dbt build`
   completed, PASS=74 WARN=0 ERROR=0 (4 table models, 64 data tests, 1 unit
   test, 5 view models).
2. Real server: `streamlit run streamlit/app.py --server.headless true` with
   the worktree warehouse boots; /_stcore/health returned 200 "ok".
3. All three pages serve over HTTP with the running app: overview -> 200,
   analysis -> 200, methodology -> 200; AppTest renders show 0 exceptions.
4. git diff --check clean.
5. Reconcile-then-override regression: narrative rendered against a synthetic
   non-default warehouse (PFM_DUCKDB_PATH) reflects 12/3/7/2 and £270.75,
   not the delivered-sample totals.

Notes
-----
* Board routing mismatch repeated: card workspace_path is a scratch dir under
  the pfm_assignment board; per board precedent implementation lives in the
  real repo /home/rafael/hermes/workspaces/pfm_assignment on an isolated
  branch wt/t_485f334d stacked on origin/main (Card 2 merged).
* The architecture graphviz is client-side (st.graphviz_chart); the module
  only ships DOT text, so no server-side graphviz dependency is introduced.
* All prose numbers come from the same dbt relations as the charts
  (mart_attribution_health sums, fct_revenue_attribution, and the
  int_unmatched_conversions diagnostic view); a page rendered against a
  different valid warehouse always reads that warehouse's own totals.
* The sample remains 100% unmatched (0 matched / 92 valid-unattributed /
  100 decided-unattributed per mart_attribution_health); the pages interpret
  that honestly and are built so matched rows render meaningfully when a
  future sample contains them (guards, empty-states, zero-filled
  vocabularies already in place from Card 2).
