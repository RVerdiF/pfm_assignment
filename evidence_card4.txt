Evidence - Card 4 (t_3b8f9d4c) - "What I'd do next": production evolution
Branch: wt/t_3b8f9d4c  (pushed to RVerdiF/pfm_assignment)
Base:   wt/t_485f334d f7d2ed4 (Card 3 approved head; its PR to main is open)

Scope delivered (all acceptance criteria map to code/docs/tests)
---------------------------------------------------------------
* streamlit/sections/next_steps.py  - new final walkthrough page
  ("What I'd do next"), pure prose by design:
    - Main message verbatim from the card: "This implementation is
      intentionally local and self-contained. In production, I would
      preserve the same transformation contracts while moving execution and
      storage to managed GCP services."
    - One-line future architecture rendered as a code block:
      "Source -> Cloud Storage/API -> BigQuery raw -> dbt -> BigQuery marts
      -> Streamlit/BI" with the supporting automation caption (Terraform +
      GitHub Actions + Cloud Run Jobs / Cloud Build + Cloud Monitoring).
    - "What changes" (six bullets, one clear purpose each):
        * Warehouse  -> BigQuery as the managed warehouse; dbt keeps owning
          the same raw -> staging -> intermediate -> marts layers.
        * dbt adapter -> dbt-bigquery; most SQL logic/contracts carry over,
          only adapter-specific adjustments.
        * Ingestion and execution -> Cloud Storage landing zone + Cloud Run
          Jobs / Cloud Build to run ingestion and dbt build, managed
          scheduling when needed.
        * Infrastructure -> Terraform (BigQuery datasets, service accounts,
          IAM, buckets/resources) with minimum permissions per stage.
        * CI/CD -> GitHub Actions (lint/tests, dbt parse/build in a
          controlled env, Terraform validation, deploys).
        * Observability -> Cloud Monitoring (freshness, ingestion failures,
          dbt failures, attribution match-rate drop, unmatched/ambiguous
          increase, abnormal shifts in non-attribution reasons).
    - "What stays the same" (three bullets): dbt transformation contracts
      and layered shape; the thin read-only consumer posture (now served
      from BigQuery marts); the already-shipped BigQuery-compatible
      sql/bigquery/attribution_health.sql asset.
    - Closing boundary caption: nothing on the page is implemented in this
      repository (no BigQuery datasets, no Terraform, no CI/CD).
    - Deliberately reads NO warehouse relation and renders NO chart, so it
      adds no relation to the consumer contract and no fictional resource.
* streamlit/app.py  - registers the page last in the navigation
  (title "What I'd do next", url_path next-steps); module docstring updated.
* docs/decisions.md  - ADR #10: "What I'd do next" is a prose-only page,
  never fictional infrastructure.
* README.md  - pages list now includes the closing page with its scope;
  repository layout line updated; pipeline consumers label aligned.

Tests (real Streamlit AppTest renders, no mocks)
------------------------------------------------
* tests/_next_steps_driver.py + tests/test_next_steps_page.py - 7 AppTest
  render checks:
    - renders with the expected header + subheaders (Where this would go /
      What changes / What stays the same);
    - is the LAST st.Page in app.py navigation (ordering assertion);
    - states the local-today + preserve-contracts main message;
    - every required production topic appears with a clear purpose
      (BigQuery, dbt-bigquery, Terraform, Cloud Storage, Cloud Run Jobs /
      Cloud Build, GitHub Actions, Cloud Monitoring; purpose phrases like
      "managed warehouse", "adapter-specific", "minimum permissions",
      "landing zone", "run ingestion", "controlled environment",
      "data freshness", "attribution match rate", "unmatched or ambiguous");
    - renders the one-line production architecture code block and the
      support caption;
    - change-vs-constant split is explicit;
    - never presents future infra as implemented (no metrics, no charts,
      source contains no execute()/read_relation()/require_connection()).
* Full suite: 56 passed (49 pre-existing + 7 new), same 12 pre-existing
  fetch_arrow_table() deprecation warnings.

Acceptance verification (real executions)
-----------------------------------------
1. Warehouse rebuilt in the isolated worktree: ingestion + `dbt build`
   completed, PASS=74 WARN=0 ERROR=0 (4 table models, 64 data tests,
   1 unit test, 5 view models).
2. Real server: `streamlit run streamlit/app.py` boots
   (/_stcore/health 200 "ok").
3. All four pages serve over HTTP with the running app: / -> 200,
   /overview -> 200, /analysis -> 200, /methodology -> 200,
   /next-steps -> 200.
4. git diff --check clean.

Notes
-----
* Board routing mismatch repeated: the card's workspace_path is the main
  pfm_assignment checkout; per board precedent implementation lives in the
  real repo on an isolated branch wt/t_3b8f9d4c stacked on the Card 3
  approved head f7d2ed4 (Card 3's PR to main is still open, so main does
  not yet carry the walkthrough pages this page joins).
* The page is intentionally relation-free; the AppTest asserts that
  invariant so a future edit cannot silently add a warehouse read or
  pretend the production pieces exist.
